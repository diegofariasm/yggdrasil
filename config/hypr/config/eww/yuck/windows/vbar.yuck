
(defwidget vbar [] (centerbox
    :class "vbar"
    :orientation "v"
    (box
      :valign "start"
      :orientation "v"
      (workspaces :orientation "v")
    )
    (box
      :space-evenly false
      :orientation "v"
      (panel_info :orientation "v")
    )

    (box
      :space-evenly false
      :orientation "v"
      :valign "end"
      :spacing 5
      (controller)
      (powermenu)
    )
  )
)
