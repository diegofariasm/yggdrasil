input {
    # You are coming to brazil.
    kb_layout = br
    
    # Set the keyboard for a good repeat rate
    repeat_rate = 50
    repeat_delay = 400

    # Focus follow mouse is annoying.
    follow_mouse = 0

    # Set the touchpad to use the superior way of scrolling
    touchpad {
        natural_scroll = yes
    }
}

