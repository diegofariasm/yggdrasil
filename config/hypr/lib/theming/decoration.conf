general {
    col.active_border = rgb($primaryColor)
    col.inactive_border = rgb($mutedColor)
}

decoration {
    # Round corners
    rounding = 5
    # Some blur. 
    blur = yes
    blur_size = 5
    blur_passes = 2
    blur_new_optimizations = on
}
