{ ... }:
{
  # A tame $HOME is a tame mind.
  xdg = {
    # We are not barbarians.
    enable = true;

    userDirs = {
      # We are not troglodites.
      # is that even a word?
      enable = true;
    };
  };
}
